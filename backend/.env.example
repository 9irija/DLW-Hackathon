OPENAI_API_KEY=your-openai-api-key-here
PORT=3001
DB_PATH=./data/audit.db
LOG_LEVEL=info

# Builder agent — Codex integration (optional)
# Use a Codex/code-specialized model for the builder (e.g. gpt-5-codex, gpt-5.2-codex, gpt-5.1-codex).
# If unset, BUILDER_MODEL is used; if both unset, defaults to gpt-4o.
# CODEX_MODEL=gpt-5-codex
# BUILDER_MODEL=gpt-4o
